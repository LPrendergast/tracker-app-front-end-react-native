import React from "react";
import { View, StyleSheet, Text } from "react-native";

const AccountScreen = () => {
  return (
    <View>
      <Text style={styles.StyleSheet}>Account Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  TextStyle: {
    fontSize: 48
  }
});

export default AccountScreen;
